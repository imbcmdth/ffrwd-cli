[package]
name = "post-rows"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.57.1"
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
# The wasi:http client bindings, matching the interface version the host
# links for a module granted -http.
wasip2 = "2.0.0"
